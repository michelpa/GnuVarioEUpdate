<form method="post" action="{{ path('adminz_variofichier_delete', {'id': vario_fichier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vario_fichier.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
